/* Additional custom styling for dilutionrisk package */

/* Home page hero section */
.hero {
  background: linear-gradient(135deg, #2c3e50 0%, #3498db 100%);
  color: white;
  padding: 60px 40px;
  border-radius: 10px;
  margin-bottom: 40px;
  text-align: center;
}

.hero h1 {
  color: white;
  font-size: 3em;
  font-weight: 700;
}

.hero p {
  font-size: 1.2em;
  opacity: 0.9;
  max-width: 800px;
  margin: 20px auto;
}

.hero .btn {
  margin: 10px 5px;
}

/* Feature cards */
.feature-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 20px;
  margin: 30px 0;
}

.feature-card {
  background: white;
  padding: 20px;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  text-align: center;
  transition: transform 0.3s ease;
}

.feature-card:hover {
  transform: translateY(-5px);
}

.feature-card .icon {
  font-size: 2.5em;
  color: #3498db;
  margin-bottom: 10px;
}

.feature-card h3 {
  color: #2c3e50;
  margin: 10px 0;
}

/* Installation code block */
.installation-block {
  background: #2c3e50;
  color: #ecf0f1;
  padding: 20px;
  border-radius: 8px;
  margin: 20px 0;
}

.installation-block code {
  color: #18bc9c;
  background: transparent;
}

/* Badge styling */
.badge-container {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin: 20px 0;
}

.badge-container img {
  height: 20px;
}